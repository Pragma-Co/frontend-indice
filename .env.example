# Copy this file to .env and adjust the value if needed:
#   cp .env.example .env
#
# Port where the Django REST backend is running on your machine.
# The backend's default port is 8000.
VITE_API_PORT=8006
